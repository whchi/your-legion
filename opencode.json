{
  "$schema": "https://opencode.ai/config.json",
  "plugin": ["context-mode", "dist/server.js"],
  "mcp": {
    "codegraph": {
      "type": "local",
      "command": ["codegraph", "serve", "--mcp"],
      "enabled": true
    }
  }
}
